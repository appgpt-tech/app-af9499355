//Source code generated by AppGPT (www.appgpt.tech)
import {
  Datagrid,
  List,
  EditButton,
  Edit,
  SimpleForm,
  Create,
  SelectColumnsButton,
  DatagridConfigurable,
  TopToolbar,
  CreateButton,
  ExportButton,
  FilterButton,
  //Field controls
  BooleanField,
  DateField,
  EmailField,
  ImageField,
  NumberField,
  ReferenceField,
  TextField,
  UrlField,
  //Input controls
  BooleanInput,
  DateInput,
  EmailInput,
  ImageInput,
  NumberInput,
  ReferenceInput,
  TextInput,
  UrlInput,
  PasswordInput,
} from 'react-admin';
import { useRecordContext } from 'react-admin';
import { Grid } from '@mui/material';
const ReadOnlyPasswordField = ({ record, source }) => {
  // You can customize the way you display the password here, e.g., mask it with asterisks
  const maskedPassword = '********';

  return <span>{maskedPassword}</span>;
};
const ListActions = () => (
  <TopToolbar>
    <FilterButton />
    <CreateButton />
    <ExportButton />
    <SelectColumnsButton />
  </TopToolbar>
);
const StagesTitle = () => {
  const record = useRecordContext();
  return <span>Stages {record ? `"${record.stageNumber}"` : ''}</span>;
};

export const StagesList = () => (
  <List actions={<ListActions />} filters={ResourceFilters}>
    <DatagridConfigurable>
      <NumberField source="stageNumber" />
      <TextField source="difficultyLevel" />
      <TextField source="environment" />
      <TextField source="objectives" />
      <EditButton />
    </DatagridConfigurable>
  </List>
);

export const StagesEdit = () => (
  <Edit title={<StagesTitle />}>
    <SimpleForm>
      <Grid container rowSpacing={1} columnSpacing={{ xs: 1, sm: 2, md: 5 }}>
        <Grid item xs={4}>
          <NumberInput source="stageNumber" />
        </Grid>
        <Grid item xs={4}>
          <TextInput source="difficultyLevel" />
        </Grid>
        <Grid item xs={4}>
          <TextInput source="environment" />
        </Grid>
        <Grid item xs={4}>
          <TextInput source="objectives" />
        </Grid>
      </Grid>
    </SimpleForm>
  </Edit>
);

export const StagesCreate = () => (
  <Create>
    <SimpleForm>
      <Grid container rowSpacing={1} columnSpacing={{ xs: 1, sm: 2, md: 5 }}>
        <Grid item xs={4}>
          <NumberInput source="stageNumber" />
        </Grid>
        <Grid item xs={4}>
          <TextInput source="difficultyLevel" />
        </Grid>
        <Grid item xs={4}>
          <TextInput source="environment" />
        </Grid>
        <Grid item xs={4}>
          <TextInput source="objectives" />
        </Grid>
      </Grid>
    </SimpleForm>
  </Create>
);

const ResourceFilters = [
  <TextInput source="q" label="Search" alwaysOn />,
  ,
  ,
  ,
  ,
];
