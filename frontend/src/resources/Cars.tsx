//Source code generated by AppGPT (www.appgpt.tech)
import {
  Datagrid,
  List,
  EditButton,
  Edit,
  SimpleForm,
  Create,
  SelectColumnsButton,
  DatagridConfigurable,
  TopToolbar,
  CreateButton,
  ExportButton,
  FilterButton,
  //Field controls
  BooleanField,
  DateField,
  EmailField,
  ImageField,
  NumberField,
  ReferenceField,
  TextField,
  UrlField,
  //Input controls
  BooleanInput,
  DateInput,
  EmailInput,
  ImageInput,
  NumberInput,
  ReferenceInput,
  TextInput,
  UrlInput,
  PasswordInput,
} from 'react-admin';
import { useRecordContext } from 'react-admin';
import { Grid } from '@mui/material';
const ReadOnlyPasswordField = ({ record, source }) => {
  // You can customize the way you display the password here, e.g., mask it with asterisks
  const maskedPassword = '********';

  return <span>{maskedPassword}</span>;
};
const ListActions = () => (
  <TopToolbar>
    <FilterButton />
    <CreateButton />
    <ExportButton />
    <SelectColumnsButton />
  </TopToolbar>
);
const CarsTitle = () => {
  const record = useRecordContext();
  return <span>Cars {record ? `"${record.model}"` : ''}</span>;
};

export const CarsList = () => (
  <List actions={<ListActions />} filters={ResourceFilters}>
    <DatagridConfigurable>
      <TextField source="model" />
      <NumberField source="speed" />
      <NumberField source="health" />
      <ReferenceField source="weaponType" reference="Weapons" />
      <EditButton />
    </DatagridConfigurable>
  </List>
);

export const CarsEdit = () => (
  <Edit title={<CarsTitle />}>
    <SimpleForm>
      <Grid container rowSpacing={1} columnSpacing={{ xs: 1, sm: 2, md: 5 }}>
        <Grid item xs={4}>
          <TextInput source="model" />
        </Grid>
        <Grid item xs={4}>
          <NumberInput source="speed" />
        </Grid>
        <Grid item xs={4}>
          <NumberInput source="health" />
        </Grid>
        <Grid item xs={4}>
          <ReferenceInput source="weaponType" reference="Weapons" />
        </Grid>
      </Grid>
    </SimpleForm>
  </Edit>
);

export const CarsCreate = () => (
  <Create>
    <SimpleForm>
      <Grid container rowSpacing={1} columnSpacing={{ xs: 1, sm: 2, md: 5 }}>
        <Grid item xs={4}>
          <TextInput source="model" />
        </Grid>
        <Grid item xs={4}>
          <NumberInput source="speed" />
        </Grid>
        <Grid item xs={4}>
          <NumberInput source="health" />
        </Grid>
        <Grid item xs={4}>
          <ReferenceInput source="weaponType" reference="Weapons" />
        </Grid>
      </Grid>
    </SimpleForm>
  </Create>
);

const ResourceFilters = [
  <TextInput source="q" label="Search" alwaysOn />,
  ,
  ,
  ,
  <ReferenceInput
    source="weaponType"
    label="weaponType"
    reference="Weapons"
    alwaysOn
  />,
];
