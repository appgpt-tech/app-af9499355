//Source code generated by AppGPT (www.appgpt.tech)
import {
  Datagrid,
  List,
  EditButton,
  Edit,
  SimpleForm,
  Create,
  SelectColumnsButton,
  DatagridConfigurable,
  TopToolbar,
  CreateButton,
  ExportButton,
  FilterButton,
  //Field controls
  BooleanField,
  DateField,
  EmailField,
  ImageField,
  NumberField,
  ReferenceField,
  TextField,
  UrlField,
  //Input controls
  BooleanInput,
  DateInput,
  EmailInput,
  ImageInput,
  NumberInput,
  ReferenceInput,
  TextInput,
  UrlInput,
  PasswordInput,
} from 'react-admin';
import { useRecordContext } from 'react-admin';
import { Grid } from '@mui/material';
const ReadOnlyPasswordField = ({ record, source }) => {
  // You can customize the way you display the password here, e.g., mask it with asterisks
  const maskedPassword = '********';

  return <span>{maskedPassword}</span>;
};
const ListActions = () => (
  <TopToolbar>
    <FilterButton />
    <CreateButton />
    <ExportButton />
    <SelectColumnsButton />
  </TopToolbar>
);
const AnimalsTitle = () => {
  const record = useRecordContext();
  return <span>Animals {record ? `"${record.type}"` : ''}</span>;
};

export const AnimalsList = () => (
  <List actions={<ListActions />} filters={ResourceFilters}>
    <DatagridConfigurable>
      <TextField source="type" />
      <NumberField source="health" />
      <NumberField source="speed" />
      <NumberField source="damage" />
      <EditButton />
    </DatagridConfigurable>
  </List>
);

export const AnimalsEdit = () => (
  <Edit title={<AnimalsTitle />}>
    <SimpleForm>
      <Grid container rowSpacing={1} columnSpacing={{ xs: 1, sm: 2, md: 5 }}>
        <Grid item xs={4}>
          <TextInput source="type" />
        </Grid>
        <Grid item xs={4}>
          <NumberInput source="health" />
        </Grid>
        <Grid item xs={4}>
          <NumberInput source="speed" />
        </Grid>
        <Grid item xs={4}>
          <NumberInput source="damage" />
        </Grid>
      </Grid>
    </SimpleForm>
  </Edit>
);

export const AnimalsCreate = () => (
  <Create>
    <SimpleForm>
      <Grid container rowSpacing={1} columnSpacing={{ xs: 1, sm: 2, md: 5 }}>
        <Grid item xs={4}>
          <TextInput source="type" />
        </Grid>
        <Grid item xs={4}>
          <NumberInput source="health" />
        </Grid>
        <Grid item xs={4}>
          <NumberInput source="speed" />
        </Grid>
        <Grid item xs={4}>
          <NumberInput source="damage" />
        </Grid>
      </Grid>
    </SimpleForm>
  </Create>
);

const ResourceFilters = [
  <TextInput source="q" label="Search" alwaysOn />,
  ,
  ,
  ,
  ,
];
